# Python — tools/build.py leaves these behind
__pycache__/
*.py[cod]

# OS / editor cruft
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp
*~

# Word lock files
~$*.docx

# API keys. Never commit.
.env
.env.local

# Downloaded candidate photographs — review set, not shipped.
# Only the chosen files, once moved into assets/img/, belong in git.
candidates/
