# Python — tools/build.py leaves these behind
__pycache__/
*.py[cod]

# OS / editor cruft
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp
*~

# Word lock files
~$*.docx

# API keys. Never commit.
.env
.env.local

# Downloaded candidate photographs — 43MB working set, not shipped.
# Only chosen files, once resized into assets/img/, belong in git.
candidates/

# Working documents and one-off scaffolding. Source copy, design history and
# the image-sourcing scripts live here: needed to build the site, not part of
# the site. Kept on disk, out of the repo.
docs/
